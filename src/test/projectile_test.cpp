//#include "Monster.h"
#include "Projectile.h"
#include "defs.h"
#include "catch.hpp"

using namespace std;
/*
TEST_CASE("Testing of Projectile::getTarget()")
{
    Projectile myProj {};
    
    CHECK_FALSE(myProj.getTarget());
    shptr<Monster> monsterptr = make_shared<Monster>();
    myProj.setTarget(monsterptr);
    //CHECK(dynamic_cast<shptr<Monster>> (myProj.getTarget()));
    myProj.setTarget(nullptr);
    CHECK(myProj.getTarget());
}
*/